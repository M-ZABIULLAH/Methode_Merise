https://github.com/M-ZABIULLAH/Methode_Merise/blob/develop/M%C3%A9thode_Merise.png
# Méthode Merise — تعریف و توضیح کامل

---

## مقدمه

**Méthode Merise** یک متدولوژی مدلسازی سیستم‌های اطلاعاتی است که در دهه 1980 در فرانسه توسعه یافت. هدف اصلی Merise، **جدا کردن داده‌ها از فرایندها** و **استفاده از چندین سطح مدل‌سازی** برای طراحی منظم، دقیق و ساخت‌یافته سیستم‌های اطلاعاتی است.

---

## سطوح اصلی در Merise

Merise شامل سه سطح برای تحلیل داده‌ها و فرایندها است:

| سطح | داده‌ها | فرایندها |
|------|---------|-----------|
| سطح مفهومی | MCD (Modèle Conceptuel de Données) | MCT (Modèle Conceptuel des Traitements) |
| سطح منطقی | MLD (Modèle Logique de Données) | MOT (Modèle Organisationnel des Traitements) |
| سطح فیزیکی | MPD (Modèle Physique de Données) | MDT (Modèle des Traitements Détaillés) |

---

##  1. MCD – Modèle Conceptuel de Données

###  هدف:
تحلیل نیازهای داده‌ای کاربران بدون در نظر گرفتن جزئیات فنی.

### عناصر اصلی:
- **Entité (موجودیت):** مانند `Client`, `Produit`, `Commande`
- **Attribut (ویژگی):** ویژگی‌هایی مانند `nom`, `email`
- **Association (ارتباط):** روابط بین موجودیت‌ها مانند `passe commande`
- **Cardinalité:** تعداد عناصر مجاز در رابطه (مانند `1`, `N`)

---

### Forme Normale de MCD
در متد **Merise**، مفهوم **Forme Normale de MCD** (یا به اختصار FNM) به معنای "شکل نرمال مدل مفهومی داده‌ها" است. هدف از این مرحله، ساده‌سازی و بهینه‌سازی مدل مفهومی داده‌ها (MCD) به گونه‌ای است که از **نواقص** و **تناقضات** احتمالی داده‌ها جلوگیری کرده و **ساختار پایگاه داده** به صورت کارآمدتر و قابل فهم‌تر تبدیل شود.

در اینجا با جزئیات بیشتر، **فرم‌های نرمال** را در مدل‌سازی مفهومی داده‌ها شرح می‌دهیم:

---

## **فرم‌های نرمال در MCD**

در **Model Conceptuel de Données (MCD)**، هدف این است که داده‌ها به‌طور بهینه طراحی شوند و از مشکلاتی مانند **تکرار داده‌ها**، **ناسازگاری** یا **وابستگی‌های اضافی** جلوگیری شود. **Forme Normale** به این فرآیند کمک می‌کند.

### **1. فرم نرمال اول (1NF - Première Forme Normale)**

####  تعریف:
در این فرم، تمام **ویژگی‌ها (Attributes)** باید اتمی باشند، یعنی هیچ ویژگی نباید خود شامل چندین مقدار باشد (مانند لیست‌ها یا مجموعه‌ها). این فرم از **جداسازی مقادیر** جلوگیری می‌کند.

#### ویژگی‌ها:
- تمامی ویژگی‌ها باید دارای مقادیر منفرد و اتمی باشند.
- هیچ ویژگی‌ای نباید تکراری باشد.

#### مثال:
اگر ویژگی `آدرس` به طور همزمان شامل **شهر**، **خیابان** و **کدپستی** باشد، باید آن را به چند ویژگی جداگانه تقسیم کنیم تا به 1NF برسیم.

---

### **2. فرم نرمال دوم (2NF - Deuxième Forme Normale)**

#### تعریف:
در این فرم، باید سیستم به **1NF** برسد و علاوه بر آن، **وابستگی‌های جزئی** بین ویژگی‌ها را از بین برد. یعنی هیچ ویژگی‌ای نباید به‌طور جزئی به کلید اصلی وابسته باشد.

#### ویژگی‌ها:
- باید ابتدا به **1NF** تبدیل شده باشیم.
- هیچ ویژگی‌ای نباید به بخشی از کلید اصلی وابسته باشد (وابستگی جزئی).

#### مثال:
اگر جدول `رزرو` دارای ویژگی‌های `id_reservation`, `id_client`, `date_reservation`, و `نام_client` باشد، وابستگی جزئی ممکن است وجود داشته باشد. زیرا `نام_client` فقط به `id_client` وابسته است و به کلید اصلی (ترکیب `id_reservation` و `id_client`) وابسته نیست. در این حالت، باید جدول‌ها را تفکیک کنیم.

---

### **3. فرم نرمال سوم (3NF - Troisième Forme Normale)**

#### تعریف:
در این فرم، سیستم باید به **2NF** رسیده باشد و **وابستگی‌های ترانزیتیو** بین ویژگی‌ها را از بین ببریم. یعنی هیچ ویژگی‌ای نباید به ویژگی دیگری از طریق یک ویژگی دیگر وابسته باشد.

#### ویژگی‌ها:
- باید ابتدا به **2NF** تبدیل شده باشیم.
- هیچ ویژگی‌ای نباید از طریق ویژگی دیگری به کلید اصلی وابسته باشد.

#### مثال:
اگر در جدول `رزرو`، ویژگی `آدرس_client` به `نام_client` وابسته باشد، وابستگی ترانزیتیو وجود دارد. زیرا `آدرس_client` از `نام_client` تاثیر می‌گیرد. برای از بین بردن این وابستگی، باید جدول `آدرس_client` را به جدولی جداگانه منتقل کنیم.

---

### **4. فرم نرمال چهارم (4NF - Quatrième Forme Normale)**

#### تعریف:
در این فرم، باید سیستم به **3NF** رسیده باشد و همچنین **وابستگی‌های چند ارزشی** را از بین ببریم. به عبارت دیگر، ویژگی‌ها نباید همزمان به چندین ویژگی دیگر وابسته باشند.

#### ویژگی‌ها:
- باید ابتدا به **3NF** تبدیل شده باشیم.
- هیچ ویژگی‌ای نباید وابستگی‌های چندگانه (Multivalued Dependencies) به ویژگی‌های دیگر داشته باشد.

#### مثال:
اگر در جدول `پرواز` ویژگی‌هایی مانند `ساعت_پرواز` و `مکان_پرواز` وجود داشته باشد که می‌توانند به طور مستقل از هم تغییر کنند، این جدول باید تفکیک شود تا از وابستگی‌های چندگانه جلوگیری شود.

---

### **5. فرم نرمال پنجم (5NF - Cinquième Forme Normale)**

#### تعریف:
در این فرم، سیستم باید به **4NF** رسیده باشد و **وابستگی‌های پیوندی** (Join Dependencies) از بین برود. این فرم بیشتر در موارد پیچیده مورد استفاده قرار می‌گیرد که در آن باید داده‌ها به نحوی ذخیره شوند که هیچ گونه اطلاعات اضافی از طریق پیوندهای مختلف ایجاد نشود.

#### ویژگی‌ها:
- باید ابتدا به **4NF** تبدیل شده باشیم.
- هیچ وابستگی پیوندی نباید وجود داشته باشد.

#### مثال:
در جدول `رزرو`، اگر هر رزرو ممکن است مربوط به چندین `بلیط` باشد و هر بلیط مربوط به یک یا چند `پرواز` باشد، در این صورت ممکن است وابستگی‌های پیوندی وجود داشته باشد که نیاز به تفکیک جداول دارد.

---

### **نتیجه‌گیری:**
استفاده از **فرم‌های نرمال** در طراحی **MCD** به کمک **Merise**، به هدف بهینه‌سازی طراحی داده‌ها و جلوگیری از مشکلاتی مانند **تکرار داده‌ها**، **وابستگی‌های غیر ضروری** و **بی‌نظمی** در پایگاه داده کمک می‌کند. هرچه مدل داده‌ها در فرم‌های نرمال بالاتری باشد، داده‌ها ساختار منظم‌تری خواهند داشت و از نظر نگهداری و مقیاس‌پذیری کارآمدتر خواهند بود.

---

## 2. MLD – Modèle Logique de Données

###  هدف:
تبدیل MCD به ساختار قابل پیاده‌سازی در پایگاه‌ داده رابطه‌ای.

### مشخصات:
- موجودیت‌ها تبدیل به **جداول** می‌شوند.
- تعیین **کلیدهای اصلی** و **خارجی**.
- تعریف روابط با استفاده از **جدول‌های واسط**.

###  مثال:
```sql
TABLE Client (
  id_client INT PRIMARY KEY,
  nom VARCHAR(100)
)
```

---
## Contraintes d’intégrité : تعریف کلی
محدودیت‌های یکپارچگی قوانینی هستند که:

صحت داده‌ها را تضمین می‌کنند.

از ناسازگاری، تکرار یا داده‌های نادرست جلوگیری می‌کنند.

در تمامی مراحل طراحی پایگاه داده (از منطقی تا فیزیکی) نقش مهمی دارند.

## 1. Contraintes d’intégrité fonctionnelle (منطقی)
این محدودیت‌ها در مرحله MLD (مدل منطقی داده‌ها) تعریف می‌شوند. این مرحله مستقل از تکنولوژی پایگاه داده است و فقط ساختار و منطق داده‌ها را مشخص می‌کند.

## انواع محدودیت‌های منطقی:
نوع	توضیح	مثال برای پروژه رزرو بلیط
Clé primaire	هر جدول باید یک کلید اصلی داشته باشد که رکوردها را یکتا کند.	id_reservation در جدول RESERVATION
Clé étrangère	ایجاد رابطه منطقی بین دو جدول.	id_passager در RESERVATION ← به PASSAGER
Unicité	جلوگیری از تکرار مقدار در یک ستون.	email در PASSAGER باید یکتا باشد.
Non-null	برخی فیلدها نباید خالی باشند.	prix در BILLET باید مقدار داشته باشد.
Domaine	نوع و دامنه مقادیر مشخص باشد.	statut فقط می‌تواند “confirmé” یا “annulé” باشد.
🔸 این محدودیت‌ها به صورت متنی یا شماتیک در مدل MLD نشان داده می‌شوند، ولی هنوز پیاده‌سازی نشده‌اند.

 2. Contraintes d’intégrité physique (فیزیکی)
این محدودیت‌ها در مرحله MPD (مدل فیزیکی داده‌ها) اجرا می‌شوند. در این مرحله، محدودیت‌ها با استفاده از دستورات SQL یا ابزار DBMS (مثل MySQL, PostgreSQL) پیاده‌سازی می‌گردند.

انواع محدودیت‌های فیزیکی:
نوع	SQL	مثال
PRIMARY KEY	PRIMARY KEY	id_passager INT PRIMARY KEY
FOREIGN KEY	FOREIGN KEY	FOREIGN KEY (id_passager) REFERENCES PASSAGER(id_passager)
UNIQUE	UNIQUE	email VARCHAR(150) UNIQUE
NOT NULL	NOT NULL	nom VARCHAR(100) NOT NULL
CHECK	CHECK	CHECK (prix > 0) یا CHECK (statut IN ('confirmé', 'annulé'))
🔸 این‌ها محدودیت‌هایی هستند که مستقیماً توسط پایگاه‌داده اعمال می‌شوند و در صورت نقض، عملیات ذخیره‌سازی را رد می‌کنند.

## 3. MPD – Modèle Physique de Données

###  هدف:
پیاده‌سازی پایگاه‌داده در یک سیستم مدیریت پایگاه‌داده خاص (مثل MySQL).

###  مشخصات:
- تعیین نوع داده‌ها: `VARCHAR`, `INT`, `DATE`, ...
- تعریف constraints، index، collation و ...

---

## 4. MCT – Modèle Conceptuel des Traitements

###  هدف:
مدلسازی فعالیت‌هایی که کاربران با سیستم انجام می‌دهند.

###  ابزارها:
- **DFD (Data Flow Diagram)**
- عملیات: ایجاد، جستجو، بروزرسانی، حذف

---

## 5. MOT – Modèle Organisationnel des Traitements

###  هدف:
مدیریت اجرای فرایندها بر اساس ساختار سازمانی.

### کاربرد:
- تعیین مسئول هر فعالیت
- توزیع وظایف بین واحدها

---

## 6. MDT – Modèle des Traitements Détaillés

###  هدف:
توضیح دقیق و فنی چگونگی اجرای فرایندها با الگوریتم‌ها و ترتیب گام‌ها.

---

## مزایای Merise

- جداسازی ساختار داده و فرایندها
- تسهیل همکاری تیمی بین تحلیل‌گران و توسعه‌دهندگان
- امکان مدیریت بهتر تغییرات
- مناسب برای پروژه‌های بزرگ و پیچیده

---